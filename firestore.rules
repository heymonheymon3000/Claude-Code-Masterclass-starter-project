rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Test mode: open access until 2026-09-20. Tighten before going to production.
      allow read, write: if request.time < timestamp.date(2026, 9, 20);
    }
  }
}
